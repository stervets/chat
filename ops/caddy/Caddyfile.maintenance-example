# Пример подключения maintenance toggle в Caddy.
# Подстрой домен и пути под сервер.

marx.core5.ru {
    encode zstd gzip

    # Точка переключения: этот import должен смотреть на активный include.
    import /opt/chat/ops/caddy/marx-active.routes.caddy
}
